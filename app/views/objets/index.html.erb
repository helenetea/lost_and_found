<p style="color: green"><%= notice %></p>

<% content_for :title, "Objets" %>

<h1>Objets</h1>

<div id="objets">
  <% @objets.each do |objet| %>
    <%= render objet %>
    <p>
      <%= link_to "Afficher l'annonce", objet %>
    </p>
  <% end %>
</div>

<%= link_to "Nouvelle annonce", new_objet_path %>
